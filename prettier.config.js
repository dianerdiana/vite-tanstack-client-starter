//  @ts-check

/** @type {import('prettier').Config} */
const config = {
  printWidth: 120,
  tabWidth: 2,
  trailingComma: 'all',
  singleQuote: true,
  semi: true,
  htmlWhitespaceSensitivity: 'ignore',
  jsxSingleQuote: true,
};

export default config;
